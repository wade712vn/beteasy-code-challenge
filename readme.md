To build and run the solution, open your shell app and change directory to project root folder

# Build solution
In project root folder, run

	dotnet build

# Run the console app
In root folder

	dotnet test
	
